[tool.poetry]
name = "my-langchain-util"
version = "0.1.0"
description = "Project providing some additional utils for Langchain."
authors = ["Quentin Reul <Quentin.H.Reul@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
guardrails-ai = "^0.4"
langchain = "^0.1"
python = "^3.11"
python-dotenv = "^1"
streamlit = "^1.31"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
